def main():
    value = 5
    show_double(value)

def show_double(number):
    result = number * 2
    print(result)

main()

# Q : parameter와 argument의 차이는 무엇일까?
# A : parameter는 매개변수로써 함수에서 정의된 변수이다
# A : argument는 전달인자로써 함수를 호출할 때 실제로 전달되는 값이다